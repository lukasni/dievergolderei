<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%= csrf_meta_tag() %>
    <%= render("favicons.html", assigns) %>
    <%= live_title_tag(assigns[:title] || "", suffix: "Die Vergolderei") %>
    <link phx-track-static rel="stylesheet" href={~p"/assets/app.css"} />
    <script defer phx-track-static type="text/javascript" src={~p"/assets/app.js"}>
    </script>
  </head>
  <body>
    <div class="container mx-auto px-4 py-4">
      <header class="">
        <section class="main-nav">
          <%= render("navigation.html", assigns) %>
        </section>
        <div class="relative text-right">
          <%= if @current_user do %>
            <div class="sm:absolute right-0 top-0 mt-1 bg-opacity-25 bg-dvgold-200 border-dvgold-400 border p-2 rounded">
              <div>
                <%= @current_user.display_name %>
              </div>
              <div>
                <hr class="my-2" />
                <%= link("Adminbereich", to: ~p"/admin", class: "block") %>
                <%= link("Ausloggen",
                  to: ~p"/sessions/#{@current_user}",
                  method: "delete",
                  class: "block"
                ) %>
              </div>
            </div>
          <% end %>
          <%= unless assigns[:hide_logo] == true do %>
            <img
              class="block lg:h-64 mt-4 mx-auto"
              src={~p(/images/header.svg)}
              alt="Logo Die Vergolderei"
            />
          <% end %>
        </div>
      </header>
      <main role="main" class="mt-8">
        <%= if info = Phoenix.Flash.get(@flash, :info) do %>
          <div class="alert alert-info">
            <p><%= info %></p>
          </div>
        <% end %>
        <%= if error = Phoenix.Flash.get(@flash, :error) do %>
          <div class="alert alert-danger">
            <p><%= error %></p>
          </div>
        <% end %>
        <%= @inner_content %>
      </main>
    </div>
  </body>
</html>
