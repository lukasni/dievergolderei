<%= breadcrumbs [@conn, :hours, @hours] %>

<h1>Öffnungszeiten Bearbeiten</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.hours_path(@conn, :update, @hours)) %>

<span><%= link "Zurück", to: Routes.hours_path(@conn, :index) %></span>
